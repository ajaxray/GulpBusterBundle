framework:
    secret: "So which of the favors of your Lord would you deny?"
    router:
        resource: "%kernel.root_dir%/config/routing.yml"
    templating:
        engines: ['twig']

# Twig Configuration
twig:
    debug:            "%kernel.debug%"
    strict_variables: "%kernel.debug%"

gulp_buster:
    busters_file: "%kernel.root_dir%/config/busters.json"